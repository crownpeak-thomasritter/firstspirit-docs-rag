# FirstSpirit / Crownpeak documentation seed list.
#
# One URL per line. Lines starting with '#' and blank lines are ignored.
# Markdown link syntax `[label](url)` is parsed — the URL is what's crawled.
#
# Add real sources here, then trigger ingestion:
#   curl -X POST http://localhost:8000/api/sources/sync -H 'Content-Type: application/json' -d '{"kind":"url_list"}'

# https://docs.e-spirit.com/odfs/
# https://docs.crownpeak.com/...
